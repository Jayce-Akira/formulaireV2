<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
    </head>
    <body>
    <h1>Demande de {{ contact['firstname'] }} {{ contact['lastname'] }}</h1>

    <p>Email : {{ contact['email'] }} </p>
    <p>Message :</p>
    <div> {{ contact['comment'] }}</div>
    </body>
</html>